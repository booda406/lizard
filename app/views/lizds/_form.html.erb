<%= form_for(@lizd) do |f| %>
  <% if @lizd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lizd.errors.count, "error") %> prohibited this lizd from being saved:</h2>

      <ul>
      <% @lizd.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
